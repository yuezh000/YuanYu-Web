services:
  web:
    build: .
    container_name: yuanyu-web-dev
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    ports:
      - "3001:3000"
    command: npm run dev
    restart: unless-stopped
